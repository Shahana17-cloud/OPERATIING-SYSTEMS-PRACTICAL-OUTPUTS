#include <stdio.h>

int main()
{
    int n, i;

    printf("Enter number of philosophers: ");
    scanf("%d", &n);

    for(i = 1; i <= n; i++)
    {
        printf("Philosopher %d is Thinking\n", i);
        printf("Philosopher %d is Eating\n", i);
    }

    return 0;
}
